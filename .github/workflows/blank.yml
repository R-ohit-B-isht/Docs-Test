# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "update-submodules"
  update-submodules:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ssh-private-key: |
            ${{ secrets.SUBMODULE_1_SSH_PRIVATE_KEY }}
            ${{ secrets.SUBMODULE_2_SSH_PRIVATE_KEY }}
          repo-mappings: |
            github.com/R-ohit-B-isht/Test_Docs_1
            github.com/R-ohit-B-isht/Test_Docs_2
          submodules: true
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}
      - name: Update Submodules
        run: |
          git submodule update --remote --merge
          git push
